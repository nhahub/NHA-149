# Environment Variables for Taqyeem Backend

# Server Configuration
PORT=5000
HOST=localhost
NODE_ENV=development

# Database Configuration
# For local development
# MONGODB_URI=mongodb://localhost:27017/taqyeem
# For production (MongoDB Atlas) - Replace with your actual connection string
MONGODB_URI=mongodb+srv://a7med-22:ahmed123@cluster0.wij54tl.mongodb.net/taqyeem?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRE=7d

# Token Configuration (for advanced token generation - currently not in use)
ACCESS_TOKEN_USER=user-access-token-secret
ACCESS_TOKEN_ADMIN=admin-access-token-secret
REFRESH_TOKEN_USER=user-refresh-token-secret
REFRESH_TOKEN_ADMIN=admin-refresh-token-secret

# Hash Configuration
SALT_ROUNDS=12

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret
CLOUDINARY_FOLDER=taqyeem

# CORS Configuration
# For production: Use your Vercel frontend URL
# For multiple origins: Separate with commas (e.g., "https://app.example.com,https://www.example.com")
FRONTEND_URL=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# WebRTC Configuration
# Note: WebRTC uses native browser APIs with Socket.io for signaling
# No external service credentials needed for basic functionality
# For production, consider adding TURN server configuration if needed:
# TURN_SERVER_URL=turn:your-turn-server.com
# TURN_SERVER_USERNAME=your-username
# TURN_SERVER_CREDENTIAL=your-credential
